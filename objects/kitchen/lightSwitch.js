import { TriggerObject } from "../../classes.js";

export const lightSwitch = new TriggerObject(
  "light switch",
  "switch1",
  ["light switch", "switch"],
  "A small light switch on the wall. It looks like it can be toggled on and off."
);

